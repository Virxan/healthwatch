# Go
/bin/
backend/*.test

# Node / frontend
frontend/node_modules/

# Built frontend assets (regenerated by `task build-frontend`) - the
# placeholder index.html stays tracked so a fresh clone still compiles;
# everything else here is build output.
backend/web/dist/*
!backend/web/dist/index.html

# Nix
result
result-*

# Container / security tool outputs
*.tar
*.tar.gz
sbom*.json
sbom*.spdx.json

# Editors / OS
.vscode/
.idea/
.DS_Store

.direnv/
.envrc.local
