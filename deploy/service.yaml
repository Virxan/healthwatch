apiVersion: v1
kind: Service
metadata:
  name: healthwatch
  namespace: healthwatch
  labels:
    app.kubernetes.io/name: healthwatch
spec:
  selector:
    app.kubernetes.io/name: healthwatch
  ports:
    - name: http
      port: 8080
      targetPort: http
